const cheerio = require('cheerio');
const { filterDataFromURL, createObjectFromDataFilter } = require('../functions');

describe('functions', ()=>{
    it('should filterDataFromURL return 200 status', async (done) => {
        
      });
})